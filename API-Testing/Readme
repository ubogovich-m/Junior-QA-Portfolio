# API Collection (JSONPlaceholder)

Эта папка содержит коллекцию API‑запросов для тестирования на основе ресурса [JSONPlaceholder](https://jsonplaceholder.typicode.com/).

## 📖 Описание
JSONPlaceholder — это бесплатный сервис для имитации REST API.  
Запросы не обновляют данные на сервере, а только **эмулируют** работу методов.

## 💻 Используемые методы
- **GET** — получение данных (например, список пользователей, постов).
- **POST** — создание новой записи (имитация).
- **PUT** — полное обновление записи.
- **PATCH** — частичное обновление записи.
- **DELETE** — удаление записи (имитация).

## 📁 CSV 

В коллекции есть папка **"CSV"** с примером параметризованных тестов.

**Чтобы запустить:**
1. Импортируйте коллекцию в Postman
2. Откройте **Runner**
3. Выберите ТОЛЬКО папку "CSV"
4. Нажмите "Select File" и загрузите `csv_data.csv`
5. Нажмите **Run**

*Файл CSV приложен в репозитории.*

## 📌 Примечания
- Все запросы работают только как **имитация**. Данные на сервере не сохраняются.  
- Сервис удобен для обучения и демонстрации тестов API.  


